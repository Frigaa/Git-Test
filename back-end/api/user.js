import { Router } from "express"


export default ({ config, db }) => {
    let router = Router()

    //Create new user

    //get all users that have 2 orders and more
    //yassine myriam
    return router
}